import React from "react";
import { ReactNode } from "react";

const DefaultDialogContentContainer = ({ children }: { children: ReactNode }) => {
    return <div>{children}</div>;
};

export default DefaultDialogContentContainer;
